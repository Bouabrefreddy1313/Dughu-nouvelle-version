# Dépendances (réinstallées dans l'image)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build Next.js (régénéré dans l'image)
.next
out
build
dist

# Env & secrets — NE JAMAIS envoyer dans le contexte Docker
.env
.env.*
!.env.example

# Git
.git
.gitignore
.gitattributes

# Docker lui-même
Dockerfile
docker-compose.yml
docker-compose.*.yml
.dockerignore

# Scripts et configuration de déploiement (exécutés sur l'hôte, pas dans l'image)
deploy

# Éditeurs / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Outillage agents / skills — inutile au runtime
.agents
.claude
.windsurf
skills-lock.json
AGENTS.md
CLAUDE.md

# Tests / couverture
coverage
*.test.ts
*.test.tsx

# Divers
README.md
*.md
*.log
.turbo
